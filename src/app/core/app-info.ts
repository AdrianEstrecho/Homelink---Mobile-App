/**
 * Single source of truth for the "which build is this?" strings the app shows
 * the user -- the splash screen footer, the side menu footer and the About the
 * App page all read from here so they can never drift apart.
 */
export const APP_VERSION = '1.0.0';

export const APP_BUILD = '2026.09';

export const APP_TAGLINE = 'Shop it. Book it. Done.';

/** Support details shown on the About pages and the Account footer. */
export const SUPPORT_PHONE = '(02) 8123-4567';
export const SUPPORT_EMAIL = 'support@homelink.com';
